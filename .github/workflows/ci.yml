name: Library Fine Calculator CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Test Library Files
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest

      - name: Run pytest
        run: pytest

  deploy:
    name: Deploy Library System
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Test-Deploy Complete
        run: |
          echo "Tests passed successfully."
          echo "Deploying application..."
          echo "test- deploy complete"
